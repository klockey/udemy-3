{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain udemy-3.keithlockey.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "dependencies": {
    "browser-sync": "^2.18.12",
    "stylelint": "^7.12.0",
    "stylelint-config-standard": "^16.0.0",
    "surge": "^0.19.0"
  }
}
